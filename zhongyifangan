A. 总体大纲（单主线）
主科学问题（唯一）

在公开可得的单味中药商品 ITS 条形码测序数据中，样本内谱系组成究竟主要表现为**“单一主导谱系 + 零散背景噪声”，还是存在“可重复、跨 marker 支持的稳定多谱系共存结构”**？(frontiersin.org
; pmc.ncbi.nlm.nih.gov
)

核心假设（可证伪）

经 marker 内阴性对照扣减、最低支持过滤和样本内谱系聚合后，大多数单味中药商品会呈现一个主导谱系显著占优的结构；只有少数样本会保留在 A/B/C 重复中可再现、且在 ITS1/ITS2 间得到一致支持的次级谱系。若多数样本在双 marker 与重复层面持续呈现多个接近强度的谱系，则该假设被否定。(frontiersin.org
; pmc.ncbi.nlm.nih.gov
)

主终点 / 次终点

主终点
每个商品样本被判定为“主导单谱系型”或“稳定多谱系型”的比例。

次终点

主导谱系相对优势度；
次级谱系在 A/B/C 文件中的重复支持率；
次级谱系在 ITS1/ITS2 间的一致支持率；
被判为“未定型”的样本比例。
以上终点都服务于同一科学问题，不引入新的副问题。(pmc.ncbi.nlm.nih.gov
; academic.oup.com
)
整体链路图（文字版）

公开 FASTQ 与阴性对照核验
→ 文件清单冻结并按 ITS1/ITS2、A/B/C、negative 分层
→ 质控、长度过滤、去重复、去噪生成 OTU
→ BLAST 注释并聚合为样本级候选谱系表
→ marker-specific 阴性对照扣减与最低支持过滤
→ 提取主导谱系与次级谱系
→ 跨 marker 整合重复证据
→ 将每个样本判定为“主导单谱系型”或“稳定多谱系型”
→ 统计总体分布并回答主科学问题。(frontiersin.org
; pmc.ncbi.nlm.nih.gov
)

B. 数据可行性核查表
1. 数据是否公开可获得

可获得。Frontiers 2024 论文的数据声明明确给出 4 个 Zenodo 记录；Zenodo 页面可直接看到原始 FASTQ 文件与 md5。(frontiersin.org
; zenodo.org
; zenodo.org
)

2. 文件构成（文件类型、命名结构、是否有对照/重复、是否有关键元数据）

可直接核到的文件类型为 .fastq。命名中可见 sample_token + replicate(A/B/C) + marker(ITS1/ITS2) 结构，例如 FM19091_A_ITS1.fastq、FM19210_B_ITS2.fastq。ITS2 数据页还直接显示 negative_..._ITS2.fastq 与 ...NEGATIVE...ITS2.fastq，说明阴性对照实际存在。可直接核到的关键元数据包括：marker、A/B/C 重复、negative 标识、md5。(zenodo.org
; zenodo.org
)

3. 数据结构（字段/记录结构/可读方式）

原始数据层是标准 FASTQ 序列记录，可直接作为序列分析输入。样本级 OTU 表、候选物种表、清洁物种表、主导/次级谱系表都可由公开 FASTQ 经标准流程生成，不依赖私有格式。(zenodo.org
)

4. 各步骤输入是否能由现有数据直接提供

能够直接提供的输入包括：

原始 reads；
ITS1/ITS2 marker 分层；
A/B/C 重复层级；
阴性对照；
通过注释生成样本内谱系组成所需的基础序列信息。(frontiersin.org
; zenodo.org
)
5. 是否存在缺失数据导致步骤无法运行

存在非致命缺失。当前公开数据页没有直接给出“FM/OO/CTI 文件 token → 人类可读药材身份”的原始级映射，也没有直接给出品牌、批次来源、加工强度和 HPTLC 对应关系。
这意味着：

不能把主线写成“标签药材是否匹配”的文件级判定；
不能做品牌差异、加工强度差异、HPTLC 对照差异分析；
能做样本内谱系结构本身的研究，因为该问题只依赖 FASTQ、negative、重复和 marker。(frontiersin.org
)
6. 若存在不可得数据，是否已删去依赖该数据的步骤

已删去。
本方案不再写入以下内容：

文件级标签对应药材的真实性判定；
品牌/来源/批次比较；
炮制/加工效应比较；
HPTLC 与单个 raw FASTQ 的一对一联动分析。
这些步骤均依赖当前未直接核到的元数据，因此不进入方案正文主线。(publicationethics.org
)
可运行性结论

可执行。
但仅限于本方案当前定义的主科学问题：研究单味中药商品样本内的谱系结构是单主导还是稳定多谱系共存。
若把问题改写成标签匹配、品牌比较、加工差异或 HPTLC 联动，则会立刻降为“部分可执行”。(frontiersin.org
)

C. 完整实验步骤（Step 1...N）
Step 1 文件清单冻结与样本分层

步骤目标（与主科学问题的关系）
建立可追溯的输入层，确保后续所有谱系结构判断都建立在真实存在的 raw FASTQ、marker、重复和 negative 分层之上，避免把不可核元数据误写进主线。

输入数据来源（必须写清）

来自原始数据：Zenodo 公开 FASTQ 文件；文件名中的 sample_token、A/B/C、ITS1/ITS2、negative 标识；md5。
不依赖上一步输出。

方法说明（不写具体参数数值，只写参数类型/调优位点）

解析文件名规则；
建立 manifest；
将文件按 marker、样本 token、重复、control flag 分组；
冻结纳入/排除规则，仅保留“可识别 marker、可识别重复层、可判断是否为 negative”的文件。

输出产物（文件/表格/指标）

manifest.tsv
sample_groups.tsv
exclusion_log.tsv

本步骤依据论文 DOI（该步骤方法直接来源）
10.3389/fphar.2024.1353434

与前一步衔接依据 DOI（证明 Step i-1 → Step i 这种衔接在文献中存在）
不适用（首步）

可执行性检查点（本步骤最可能失败点）
文件名无法稳定解析，或存在 marker/重复/negative 标识缺失。

若失败的降级路径（仍保持同一主线，不引入新主线）
排除无法解析文件，仅用可解析文件进入后续步骤，不额外补造样本身份字段。(frontiersin.org
; zenodo.org
; zenodo.org
)

Step 2 质控、长度过滤、去重复与去噪，生成 marker 内 OTU 表

步骤目标（与主科学问题的关系）
先把原始 reads 中的明显噪声、异常长度序列与冗余序列剔除，得到可用于描述样本内真实谱系结构的 marker 内 OTU 表。

输入数据来源（必须写清）

来自原始数据：Step 1 的 manifest.tsv 所对应的 FASTQ 文件。
来自上一步输出：sample_groups.tsv。

方法说明（不写具体参数数值，只写参数类型/调优位点）

按 ITS1、ITS2 分开处理；
进行质量筛选；
按 marker 设定长度区间类型；
去重复；
使用 UNOISE 类去噪算法生成 OTU；
冻结所有过滤类型与去噪位点，不做结果导向回调。

输出产物（文件/表格/指标）

ITS1_otu_table.tsv
ITS2_otu_table.tsv
qc_summary.tsv

本步骤依据论文 DOI（该步骤方法直接来源）
10.1038/nmeth.3869

与前一步衔接依据 DOI（证明 Step i-1 → Step i 这种衔接在文献中存在）
10.3389/fphar.2024.1353434

可执行性检查点（本步骤最可能失败点）
某些文件 reads 过少，或某一 marker 的大量样本在过滤后信号不足。

若失败的降级路径（仍保持同一主线，不引入新主线）
允许样本以单 marker 进入后续，但在最终判定中仅能获得“单 marker 证据”，不能判为“稳定多谱系型”。(pmc.ncbi.nlm.nih.gov
; frontiersin.org
)

Step 3 BLAST 注释，生成样本级候选谱系表

步骤目标（与主科学问题的关系）
把去噪后 OTU 转成可解释的候选谱系标签，使“主导谱系”和“次级谱系”能够在生物学层面被定义。

输入数据来源（必须写清）

来自上一步输出：ITS1_otu_table.tsv、ITS2_otu_table.tsv。
来自原始数据外部资源：公共参考数据库的可比对序列库。

方法说明（不写具体参数数值，只写参数类型/调优位点）

对每个 marker 的 OTU 执行 BLAST 型相似性注释；
记录 top hit、注释层级、相似性与覆盖相关指标；
保留不确定注释状态，用于后续降级，而不是强行给出物种级标签。

输出产物（文件/表格/指标）

ITS1_tax_assign.tsv
ITS2_tax_assign.tsv
annotation_confidence.tsv

本步骤依据论文 DOI（该步骤方法直接来源）
10.1186/1471-2105-10-421

与前一步衔接依据 DOI（证明 Step i-1 → Step i 这种衔接在文献中存在）
10.3389/fphar.2024.1353434

可执行性检查点（本步骤最可能失败点）
数据库命中不稳定，或大量 OTU 只能到属级/科级。

若失败的降级路径（仍保持同一主线，不引入新主线）
允许把“主导/次级谱系”判定降到属级，不强迫物种级解释。(pmc.ncbi.nlm.nih.gov
)

Step 4 谱系聚合、marker-specific 阴性对照扣减与最低支持过滤，形成清洁物种表

步骤目标（与主科学问题的关系）
把候选谱系表转成真正可用于回答主问题的“清洁样本级谱系表”，尽可能剔除背景污染、tag-jump 与极低支持噪声。

输入数据来源（必须写清）

来自上一步输出：ITS1_tax_assign.tsv、ITS2_tax_assign.tsv、annotation_confidence.tsv。
来自原始数据：negative FASTQ 对应的 control 组。

方法说明（不写具体参数数值，只写参数类型/调优位点）

使用 ASAP 类谱系聚合策略，合并可归并的候选条目；
在 ITS1、ITS2 内分别进行 negative subtraction；
应用最低支持阈值类型；
输出每个样本在每个 marker 下的清洁谱系丰度表。

输出产物（文件/表格/指标）

ITS1_clean_taxa.tsv
ITS2_clean_taxa.tsv
negative_subtraction_log.tsv

本步骤依据论文 DOI（该步骤方法直接来源）
10.1111/1755-0998.13281

与前一步衔接依据 DOI（证明 Step i-1 → Step i 这种衔接在文献中存在）
10.3389/fphar.2023.1305410

可执行性检查点（本步骤最可能失败点）
negative 样本本身背景偏高，导致真实弱信号与背景难区分。

若失败的降级路径（仍保持同一主线，不引入新主线）
提高保守性，仅保留高置信主导谱系；对次级谱系统一降为“未定”，不强行判入稳定共存。(pmc.ncbi.nlm.nih.gov
; pmc.ncbi.nlm.nih.gov
)

Step 5 提取样本内主导谱系与次级谱系，并进行重复层稳定性判定

步骤目标（与主科学问题的关系）
把“样本里有什么”进一步转成“样本结构是什么”：即识别主导谱系是否明显占优，以及次级谱系是否只是一过性噪声还是在 A/B/C 中可重复出现。

输入数据来源（必须写清）

来自上一步输出：ITS1_clean_taxa.tsv、ITS2_clean_taxa.tsv。
来自 Step 1 输出：sample_groups.tsv 中的 A/B/C 重复分组信息。

方法说明（不写具体参数数值，只写参数类型/调优位点）

在每个 marker 内计算样本的谱系优势度、谱系数与 rank-abundance 结构；
抽取主导谱系；
对非主导谱系进行重复支持统计；
仅把在重复层面满足稳定出现条件的非主导谱系记为“次级谱系候选”。

输出产物（文件/表格/指标）

sample_dominant_lineage.tsv
sample_secondary_candidates.tsv
replicate_support_summary.tsv

本步骤依据论文 DOI（该步骤方法直接来源）
10.3390/plants10102120

与前一步衔接依据 DOI（证明 Step i-1 → Step i 这种衔接在文献中存在）
10.3390/plants10102120

可执行性检查点（本步骤最可能失败点）
同一样本 A/B/C 差异过大，导致无法区分真实次级谱系与随机弱信号。

若失败的降级路径（仍保持同一主线，不引入新主线）
把样本暂列为“未定型”，只保留主导谱系结论，不给出稳定多谱系判定。(pmc.ncbi.nlm.nih.gov
)

Step 6 跨 marker 整合证据，判定样本类型并完成终点统计

步骤目标（与主科学问题的关系）
把 ITS1 与 ITS2 的结果合并到样本层，最终回答：各商品究竟更接近主导单谱系，还是存在稳定多谱系共存。

输入数据来源（必须写清）

来自上一步输出：sample_dominant_lineage.tsv、sample_secondary_candidates.tsv、replicate_support_summary.tsv。
来自上一步输出：ITS1_clean_taxa.tsv、ITS2_clean_taxa.tsv。

方法说明（不写具体参数数值，只写参数类型/调优位点）

整合 ITS1 与 ITS2 对主导谱系的支持；
仅当次级谱系同时满足“重复层稳定”与“跨 marker 支持”时，才将样本归为“稳定多谱系型”；
若仅满足其一，则记为“单 marker/单层次 tentative”；
对全部样本进行主终点与次终点统计。

输出产物（文件/表格/指标）

sample_type_call.tsv
primary_endpoint_summary.tsv
secondary_endpoint_summary.tsv
classification_log.tsv

本步骤依据论文 DOI（该步骤方法直接来源）
10.1093/gigascience/gix080

与前一步衔接依据 DOI（证明 Step i-1 → Step i 这种衔接在文献中存在）
10.1093/gigascience/gix080

可执行性检查点（本步骤最可能失败点）
ITS1 与 ITS2 对同一样本的次级谱系支持不一致。

若失败的降级路径（仍保持同一主线，不引入新主线）
保留“主导单谱系型 / 未定型”二分类，不把样本提升为“稳定多谱系型”。(academic.oup.com
; pmc.ncbi.nlm.nih.gov
)
