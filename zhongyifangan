方法学（Methods）
1 研究设计与总体策略
本研究旨在基于公开可获得的ITS条形码测序数据，评估中药单味商品样本中“标签对应谱系信号是否主导”以及“是否存在可支持的次级谱系候选信号”。考虑到公开数据层级与原始实验层级之间存在信息不对称（例如公开为单文件FASTQ输入层，而原始实验流程包含paired-end测序与后续合并步骤），本研究采用“原文约束下的双轨再分析策略”：
（1）同构最小复核轨（Track 1）：尽可能贴近原研究报告的核心分析语义，验证关键结论是否可被公开数据支持；
（2）保守扩展评估轨（Track 2）：在同构轨基础上增加一致性与分级约束，对次级谱系信号进行保守识别与不确定性标注。
本研究不宣称对原始实验流程进行逐步完全复刻，而是强调在公开数据约束下的可复核与可审计再分析。
________________________________________
2 数据来源与数据层级说明
2.1 数据来源
研究数据来源于公开数据库中与目标研究对应的ITS1/ITS2 FASTQ文件集合（含样本文件与阴性对照文件），并对全部文件记录文件名、文件大小与校验信息（如可用）。
2.2 数据层级说明
公开文件作为“单文件输入层”用于再分析。依据原研究方法描述，原始实验层包含MiSeq paired-end读段及其合并流程；因此本研究在方法表述中将“公开输入层”与“原始测序层”明确区分，避免将公开文件误写为原始单端测序输入。
________________________________________
3 数据治理与样本映射
3.1 文件清单构建
构建统一清单（manifest），至少包含：
•	file_id（唯一文件标识）
•	marker（ITS1/ITS2）
•	sample_token（文件名中的样本编码）
•	replicate_proxy（A/B/C标签）
•	control_flag（negative/非negative）
•	source_batch（来源入口）
3.2 样本身份映射
构建样本映射表（sample_map），将文件名编码映射至研究样本身份，并区分：
1.	主分析集合（商业单味样本）；
2.	阴性对照集合；
3.	其他非主集合对象（如无法映射至主样本叙事者）。
仅当文件满足“可校验、可识别marker、可识别样本身份”三项条件时纳入主分析流程。
3.3 映射审计输出
在正式分析前输出“数据可用性审计表”，报告：
•	总文件数与纳入文件数；
•	各marker文件覆盖度；
•	主样本与对照样本数量；
•	被排除文件及排除原因。
该步骤用于保证后续样本级解释建立在可追溯映射基础上。
________________________________________
4 预处理与特征构建
4.1 质量控制与长度约束
对ITS1/ITS2分别进行质量控制与长度约束，参数在补充材料中完整披露。为避免方法漂移，所有参数在正式统计前冻结，不随结果进行后验调整。
4.2 去噪与序列特征单位（同构约束）
Track 1 中该步骤固定为 UNOISE 生成 OTUs，并保持与原研究方法链一致的流程语义；不在 Track 1 中使用替代去噪算法。任何因公开数据限制而发生的方法偏离，均不再称为“功能等价实现”，而应在“同构偏离登记表”中逐项记录并在结果中标注其影响范围。
4.3 分类注释
将保留特征与参考数据库进行比对注释，记录每条注释的匹配质量指标（例如相似度、覆盖度、注释层级），为后续证据分级提供依据。
________________________________________
5 阴性对照扣减与阈值规则
5.1 marker-specific对照处理
ITS1与ITS2分别使用对应阴性对照文件进行背景识别与扣减，不进行跨marker替代。若某marker对照质量受限，则触发保守规则（提高保留阈值或降级证据等级），而非简单视为“无对照”。
5.2 最低支持阈值
对扣减后信号应用最低支持阈值（reads或等价支持量阈值），以减少偶然低丰度噪声进入解释层。阈值设定遵循“先验冻结”原则，不依据目标结果动态调参。
5.3 replicate proxy使用边界
A/B/C标签定义为replicate proxy（复孔代理），仅用于稳健性标注（是否跨文件重复出现），不用于推断严格实验方差参数，也不据此进行过度统计外推。
________________________________________
6 双轨分析实施
6.1 Track 1：严格同构复核（primary replication track）
Track 1 采用“分层同构执行”：
- 若数据具备 raw paired-end 且含 demultiplex 所需信息，则执行完整原链：PEAR→ngsfilter→obigrep→去重复→UNOISE→BLAST→阴性对照扣减→reads≥5→ASAP→unique species；
- 若公开层仅提供已拆分单文件 FASTQ，则 Track 1 起点定义为“demultiplex 之后的同构子链”，执行：obigrep→去重复→UNOISE→BLAST→阴性对照扣减→reads≥5→ASAP→unique species，并将该情形标记为“Sub-chain replication（非全链同构）”。
Primary replication 结论仅来自“全链同构样本”；Sub-chain 结果单列为次级复核。
Track 1 的步骤顺序与参数在分析前冻结。若公开数据层级导致任一步无法原样执行，必须进入“同构偏离登记表（deviation log）”并在主文结果中单列说明。
6.2Track 2：保守扩展评估（exploratory track）
Track 2 为探索性附轨，且执行“正文降权规则”：
1) 主文结果与摘要仅报告 Track 1；
2) Track 2 全部放入补充材料，正文最多保留 1 段趋势性描述；
3) 凡“仅 Track 2 支持、Track 1 不支持”的条目，统一标记 exploratory，不进入主结论与标题结论。
________________________________________
7 证据分级与结果判定
为兼顾可解释性与保守性，设置三级证据标签：
•	Level A（高可信标签主导）：标签相关信号在对照扣减后稳定存在，并满足最低支持阈值。
•	- Level B（探索性候选信号）：非标签信号在对照扣减后仍保留，且满足最低一致性约束；该等级仅表示“可复核的弱信号”，不作成分存在性推断，不作机制推断，不作临床或质量风险推断。Level B 必须同时报告 reads 规模、对照背景比值、阈值敏感性和注释置信度；任一缺失则降为 Level C。
•	Level C（信息不足/不确定）：仅边缘信号、孤立单文件信号，或受对照背景影响较大。
其中Level B仅表征“候选信号”，不直接等同于已证实的实际配方复杂性。
________________________________________
8 统计与可视化
8.1 描述性统计
报告各样本在ITS1/ITS2下的保留特征数、标签相关信号比例、非标签信号比例，以及不同证据等级分布。
8.2 一致性评估
对A/B/C文件层重复观测进行一致性统计（如出现频次、Jaccard相似度或等价指标），并在样本层汇总。
8.3 可视化
建议输出：
•	数据治理流程图（纳入/排除路径）；
•	marker分层结果柱状图；
•	样本×证据等级热图；
•	标签主导与候选次级信号的对照图。
所有图表对应的输入表与生成脚本在补充材料公开。
________________________________________
9 偏倚控制与敏感性分析
9.1 主要偏倚来源
预设并报告以下偏倚风险：
1.	PCR扩增偏倚；
2.	参考数据库注释误差；
3.	样品加工导致DNA降解；
4.	公开中间层数据与原始层不一致带来的可复现边界。
9.2 敏感性分析
至少执行以下敏感性检查：
•	阈值上下浮动对Level A/B/C分布影响；
•	是否要求“跨marker同时支持”对Level B数量影响；
•	对照扣减强度变化对非标签信号保留率影响。
若结论仅在狭窄参数区间成立，则降级为探索性发现。
________________________________________
10 可重复性与研究透明度
1.	公开完整分析清单（manifest、sample_map、结果表）；
2.	固化参数文件与脚本版本；
3.	报告每一步纳入/排除理由；
4.	区分“复核结论”与“扩展结论”；
5.	在讨论中对Level B与Level C采用保守措辞，避免因方法学构造导致过度科学外推。

