修订差异清单
1) 输入字段一致性修订
已逐项检查主文 Fig.1–Fig.5，处理结果如下：
•	删除所有非文件原生的占位布尔字段作为“输入字段”
以下字段不再出现在“输入字段”列：
has_dom_ITS1, has_dom_ITS2, dom_match, dom_conflict, cross_secondary_supported, single_marker_clear, call_stability, single_marker_dropout_class, dominant_share, max_secondary_share。
它们现在统一改为：绘图汇总/统计派生量，不再当作源数据字段申报。
•	Fig.2C 输入字段修订
原写法中的 raw_reads 删除。
现统一为文件原生字段：
ITS2_candidate_lineages.tsv 使用 reads；
ITS2_clean_lineages.tsv 使用 clean_reads；
ITS2_negative_profile.tsv / ITS_aux_control_profile.tsv 使用 reads。
散点图中“净化前 vs 净化后”由 candidate.reads 与 clean.clean_reads 对接生成。
•	Fig.3C 输入字段修订
原写法中的 dominant_clean_reads、secondary_clean_reads 不再列为独立输入字段。
现改为直接使用 dominant / secondary 表中的文件原生 clean_reads，
dominant_share 与 max_secondary_share 仅作为绘图计算量。
•	Fig.4A / Fig.4C 输入字段修订
不再把证据路径布尔变量列为输入字段。
现只保留文件原生字段：
sample_id, final_type, reason_code，以及 dominant / secondary 输出表中的 taxon_rank, taxon_name, clean_reads。
证据路径由绘图时按 Step 6 规则聚合。
•	Fig.5B / Fig.5D 输入字段修订
call_stability 与 single_marker_dropout_class 从“输入字段”移除，
仅保留 sensitivity_grid_summary.tsv 或 sample_type_call.tsv 中已定义字段，
图中稳定性与单-marker 情形由汇总逻辑产生，不再冒充源字段。
结论：主文图的“输入字段”现在只保留来自已列明文件的原生字段；所有便于作图的中间占位量已降级为汇总逻辑，不再作为输入字段申报。
________________________________________
2) 占位引用替换为可点击完整链接
以下占位引用已替换为可点击完整链接：
•	Frontiers 2024 全文
•	BMC Genomics 2025 全文
•	GitHub：zhongyifangan
•	GitHub：细则
文献核对结论不变：Frontiers 2024 的 Fig.1 是方法比较 Venn 图；BMC Genomics 2025 的 Fig.1 是分析流程总览，二者都不是审计图。 (Frontiers)
________________________________________
3) 新增“不可替代性说明”（逐子图补充）
Fig.1
•	A 不可替代性说明：必须在主文，用来定义 ITS1 vs ITS2、marker-specific 处理与终判逻辑这三个阅读坐标；若移入补充，后续所有结果图都会失去解释基线。
•	B 不可替代性说明：必须在主文，因为它把“证据如何流向 final_type”直接可视化，是全文主问题的最短证据路径。
•	C 不可替代性说明：必须在主文，因为它把关键比较轴与终判类别直接对应，是审稿人判断框架是否合理的首个落点。
Fig.2
•	A 不可替代性说明：必须在主文，因为原始支持量到 clean 支持量的变化是后续一切谱系解释成立的前提。
•	B 不可替代性说明：必须在主文，因为 feature 复杂度是否被过度压缩会直接影响审稿人对过滤策略的信任。
•	C 不可替代性说明：必须在主文，因为 ITS2 的 NEGATIVE/AUX_CONTROL 处理是方法学关键差异，不是附属审计信息。
•	D 不可替代性说明：必须在主文，因为“候选命中”与“进入解释层的净化命中”不是一回事，这一差异属于主结论必要前提。
Fig.3
•	A 不可替代性说明：必须在主文，因为 dominant 结构是样本解释的第一层结果，不是背景细节。
•	B 不可替代性说明：必须在主文，因为 secondary 是否真实存在是论文核心发现之一，不能藏在补充。
•	C 不可替代性说明：必须在主文，因为它直接区分“单主导样本”和“多谱系候选样本”，决定后续终判的阅读方式。
•	D 不可替代性说明：必须在主文，因为 replicate 支持是 secondary 能否进入解释层的门槛，不是单纯质量附录。
Fig.4
•	A 不可替代性说明：必须在主文，因为 final_type 的形成逻辑就是论文 primary endpoint 的核心证据链。
•	B 不可替代性说明：必须在主文，因为终判类别分布本身就是研究结论，而不是补充统计。
•	C 不可替代性说明：必须在主文，因为它把终判从“标签”变成“可复核证据结构”，直接降低黑箱感。
•	D 不可替代性说明：仅在补齐标签参考表后才进入主文；若缺失输入，则不绘制，不以下降主文可信度。
Fig.5
•	A 不可替代性说明：必须在主文，因为审稿人首先会问“阈值改动后结论是否仍成立”。
•	B 不可替代性说明：必须在主文，因为真正脆弱的样本需要被正面暴露，不能用总体均值掩盖。
•	C 不可替代性说明：必须在主文，因为“未定型”的来源决定其解释价值，不应被误读为失败样本堆。
•	D 不可替代性说明：必须在主文，因为单-marker 边界是本研究方法学自我约束的关键证据。
________________________________________
4) 主文图 / 补充图边界表
图号	放置位置	放置理由
Fig.1	Main text	定义研究问题、分析框架与关键比较轴；承担全文首图叙事位
Fig.2	Main text	证明质量控制与 marker-specific 处理有效，直接决定结果可信度
Fig.3	Main text	展示 dominant / stable secondary 的核心生物学证据层
Fig.4	Main text	展示跨 marker 终判的 primary endpoint 与 reason_code 结构
Fig.5	Main text	回应稳健性与边界问题，直接对应高频审稿追问
Fig.S1	Supplementary	仅支撑可复现性与方法审计，不承担主结果叙事
Table.S1	Supplementary	审计字典与错误码登记，属于 Methods / 复现支持材料
Table.S2	Supplementary	主图—输入文件交叉索引，属于复现支撑而非科学发现
________________________________________
5) 审计型展示复核结果
•	未发现仍留在主文的审计型子图。
•	主文中已不再出现以下内容：
manifest 纳入统计、E001–E007 错误码分布、read_path/path audit、文件冻结/纳入剔除流程图。
•	上述内容继续只保留在 Fig.S1 / Table.S1。
•	因此：本轮无需新增迁移；主文与补充的边界已符合“非审计首图、审计下移”的要求。

